services:
  seerr:
    image: ghcr.io/seerr-team/seerr:latest
    container_name: seerr
    init: true
    environment:
      - TZ=America/Fortaleza   # Set your timezone here
    ports:
      - 5055:5055
    volumes:
      - /home/titan/seerr/config:/app/config
    restart: unless-stopped
